ANTHROPIC_API_KEY=""
LLM_API_KEY=""
#Điền 1 trong 2 key thou nhá ae hoặc xoá cái anthropic đi cũng được
#Base url thì xài provider nào thì đi check base url của provider đó, của deepseek là https://api.deepseek.com
#gemini base url thì là https://generativelanguage.googleapis.com/v1beta/openai/
LLM_BASE_URL=

# Model to use (default: gemini-2.5-flash)
LLM_MODEL=

# Temperature: 0.0 = deterministic, 1.0 = creative
# Keep low (0.1) for structured JSON outputs
LLM_TEMPERATURE=0.1

# Max tokens in LLM output (8192 covers all endpoints including stage5-synthesize)
LLM_MAX_OUTPUT_TOKENS=8192

# Confidence thresholds
# BR-VER-03: portfolio evaluation pass threshold
PORTFOLIO_EVAL_THRESHOLD=0.85

# BR-DIS-05: dispute auto-resolution threshold (NestJS reads this from ai-service response)
DISPUTE_EVAL_THRESHOLD=0.80

# Service
PORT=8000
ENV=development
